# XIRA Oracle Wallet (X Layer Testnet)
# FUND THIS: https://www.okx.com/xlayer/faucet
# NEVER commit the private key. Set it in Render dashboard / local .env.
PRIVATE_KEY=<redacted-set-via-render-secret>
ADDRESS=0x0CE306F2863a98e847F454dF74E93Ff1461ED3c0

# After funding, deploy the contract:
# cd contracts
# export PRIVATE_KEY=<redacted-set-via-render-secret>
# forge script script/DeployAll.s.sol --rpc-url https://testrpc.xlayer.tech --broadcast --legacy
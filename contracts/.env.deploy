# XIRA Deployment Wallet (X Layer Testnet)
# FUND THIS: https://www.okx.com/xlayer/faucet
#
# SECURITY NOTE: the previous key that lived here was committed in repository
# history and is considered leaked. It has been removed from the working tree.
# Do NOT reuse it. Generate a fresh key with:
#   cast wallet new
# Fund the new address, authorize it as an updater:
#   cast send <CONTRACT_ADDRESS> "setAuthorizedUpdater(address,bool)" \
#     <NEW_ADDRESS> true --rpc-url https://testrpc.xlayer.tech --private-key $PRIVATE_KEY
# Then set PRIVATE_KEY in the Render dashboard secret, never in this file.
PRIVATE_KEY=<redacted-set-via-render-secret>
ADDRESS=0x5368FB097E57F34020A8FAAA52a242eeF814f8AC

# After funding, deploy the contract:
# cd contracts
# export PRIVATE_KEY=<redacted-set-via-render-secret>
# forge script script/DeployAll.s.sol --rpc-url https://testrpc.xlayer.tech --broadcast --legacy